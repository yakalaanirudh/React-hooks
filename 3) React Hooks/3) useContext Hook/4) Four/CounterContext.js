//When we createContext it creates two things ContextProvider and ContextConsumer
import { createContext } from 'react'

export const CounterContext=createContext(null)             //null is initial value